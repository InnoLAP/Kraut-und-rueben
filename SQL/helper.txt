NOT ALLOWED
Rezept 1-3 &&  Zutat 1-8: Vegetarisch
Rezept 2-5 &&  Zutat 4-10: Vegan
Rezept 4-8 &&  Zutat 6-18: Atkins
Rezept 4-10 && Zutat 1-20: The Zone

Rezept 1-5 &&  Zutat 1-3: Allergie 1
Rezept 8-10 && Zutat 9-20: Allergie 2

-------------------------------------------------------------------
<-- Select all ZUTATEN allowed for ALLERGIE restriction XY [WORKS WITH REAL DATA]-->
SELECT
	ZUTAT.ZUTATENNR,
	ZUTAT.BEZEICHNUNG,
 	ZUTAT.EINHEIT,
 	ZUTAT.NETTOPREIS,
 	ZUTAT.BESTAND,
 	ZUTAT.LIEFERANT,
 	ZUTAT.KALORIEN,
 	ZUTAT.KOHLENHYDRATE,
 	ZUTAT.PROTEIN
FROM ZUTAT
LEFT JOIN
	(SELECT DISTINCT ALLERGIEZUTAT.ZUTATENNR
     FROM ALLERGIEZUTAT
     WHERE ALLERGIEZUTAT.ALLERGIENR = 1) AS tTemp //*
ON ZUTAT.ZUTATENNR = tTemp.ZUTATENNR
WHERE tTemp.ZUTATENNR IS null

-------------------------------------------------------------------
<-- Select all REZEPTE allowed for ALLERGIE restriction XY [WORKS WITH REAL DATA]-->
SELECT
	REZEPTE.REZEPTNR,
	REZEPTE.REZEPTNAME,
	REZEPTE.REZEPTLINK
FROM REZEPTE
LEFT JOIN
	(SELECT DISTINCT ALLERGIEREZEPTE.REZEPTNR
     FROM ALLERGIEREZEPTE
     WHERE ALLERGIEREZEPTE.ALLERGIENR = 2) AS tTemp
ON REZEPTE.REZEPTNR = tTemp.REZEPTNR
WHERE tTemp.REZEPTNR IS null

-------------------------------------------------------------------
<-- Select all ZUTATEN allowed for DIET restriction XY [WORKS WITH REAL DATA]-->
SELECT
	ZUTAT.ZUTATENNR,
	ZUTAT.BEZEICHNUNG,
 	ZUTAT.EINHEIT,
 	ZUTAT.NETTOPREIS,
 	ZUTAT.BESTAND,
 	ZUTAT.LIEFERANT,
 	ZUTAT.KALORIEN,
 	ZUTAT.KOHLENHYDRATE,
 	ZUTAT.PROTEIN
FROM ZUTAT
LEFT JOIN
	(SELECT DISTINCT DIETZUTAT.ZUTATENNR
     FROM DIETZUTAT
     WHERE DIETZUTAT.DIETNR = 2) AS tTemp //*
ON ZUTAT.ZUTATENNR = tTemp.ZUTATENNR
WHERE tTemp.ZUTATENNR IS null

-------------------------------------------------------------------
<-- Select all REZEPTE allowed for DIET restriction XY [WORKS WITH REAL DATA]-->
SELECT
	REZEPTE.REZEPTNR,
	REZEPTE.REZEPTNAME,
	REZEPTE.REZEPTLINK
FROM REZEPTE
LEFT JOIN
	(SELECT DISTINCT DIETREZEPTE.REZEPTNR
     FROM DIETREZEPTE
     WHERE DIETREZEPTE.DIETNR = 3) AS tTemp //*
ON REZEPTE.REZEPTNR = tTemp.REZEPTNR
WHERE tTemp.REZEPTNR IS null

-------------------------------------------------------------------
<-- Select all ZUTATEN allowed for DIET restriction XY AND ALLERGIE restriction XY [WORKS WITH REAL DATA]-->
SELECT * from
	(SELECT
		ZUTAT.ZUTATENNR,
		ZUTAT.BEZEICHNUNG,
 		ZUTAT.EINHEIT,
 		ZUTAT.NETTOPREIS,
 		ZUTAT.BESTAND,
 		ZUTAT.LIEFERANT,
 		ZUTAT.KALORIEN,
 		ZUTAT.KOHLENHYDRATE,
 		ZUTAT.PROTEIN
	FROM ZUTAT
	LEFT JOIN
		(SELECT DISTINCT ALLERGIEZUTAT.ZUTATENNR
     	FROM ALLERGIEZUTAT
     	WHERE ALLERGIEZUTAT.ALLERGIENR = 1) AS tTemp //*
	ON ZUTAT.ZUTATENNR = tTemp.ZUTATENNR
	WHERE tTemp.ZUTATENNR IS null) as t2Temp
LEFT JOIN
	(SELECT
		ZUTAT.ZUTATENNR,
		ZUTAT.BEZEICHNUNG,
 		ZUTAT.EINHEIT,
 		ZUTAT.NETTOPREIS,
 		ZUTAT.BESTAND,
 		ZUTAT.LIEFERANT,
 		ZUTAT.KALORIEN,
 		ZUTAT.KOHLENHYDRATE,
 		ZUTAT.PROTEIN
	FROM ZUTAT
	LEFT JOIN
		(SELECT DISTINCT DIETZUTAT.ZUTATENNR
     	FROM DIETZUTAT
     	WHERE DIETZUTAT.DIETNR = 3) AS tTemp //*
	ON ZUTAT.ZUTATENNR = tTemp.ZUTATENNR
	WHERE tTemp.ZUTATENNR IS null) as t3Temp
ON t2Temp.BEZEICHNUNG = t3Temp.BEZEICHNUNG
WHERE t2Temp.BEZEICHNUNG IS NOT null AND t3Temp.BEZEICHNUNG IS NOT null

-------------------------------------------------------------------
<-- Select all REZEPTE allowed for DIET restriction XY AND ALLERGIE restriction XY [WORKS WITH REAL DATA]-->
SELECT * from
	(SELECT
		REZEPTE.REZEPTNR,
		REZEPTE.REZEPTNAME,
 		REZEPTE.REZEPTLINK
	FROM REZEPTE
	LEFT JOIN
		(SELECT DISTINCT ALLERGIEREZEPTE.REZEPTNR
     	FROM ALLERGIEREZEPTE
     	WHERE ALLERGIEREZEPTE.ALLERGIENR = 1) AS tTemp //*
	ON REZEPTE.REZEPTNR = tTemp.REZEPTNR
	WHERE tTemp.REZEPTNR IS null) as t2Temp
LEFT JOIN
	(SELECT
		REZEPTE.REZEPTNR,
		REZEPTE.REZEPTNAME,
 		REZEPTE.REZEPTLINK
	FROM REZEPTE
	LEFT JOIN
		(SELECT DISTINCT DIETREZEPTE.REZEPTNR
     	FROM DIETREZEPTE
     	WHERE DIETREZEPTE.DIETNR = 4) AS tTemp //*
	ON REZEPTE.REZEPTNR = tTemp.REZEPTNR
	WHERE tTemp.REZEPTNR IS null) as t3Temp
ON t2Temp.REZEPTNAME = t3Temp.REZEPTNAME
WHERE t2Temp.REZEPTNAME IS NOT null AND t3Temp.REZEPTNAME IS NOT null

---------------------------------------------------------------------
<------- join tables REZEPTEZUTAT, ZUTAT and REZEPTE (no Filter) [NEEDED?]----->
SELECT
	ZUTAT.BEZEICHNUNG,
	ZUTAT.NETTOPREIS,
	ZUTAT.KALORIEN,
	ZUTAT.KOHLENHYDRATE,
	ZUTAT.PROTEIN,
	REZEPTEZUTAT.REZEPTNR,
	REZEPTEZUTAT.MENGE,
	REZEPTE.REZEPTNAME,
	REZEPTE.REZEPTLINK
FROM REZEPTEZUTAT 
JOIN ZUTAT 
	ON REZEPTEZUTAT.ZUTATENNR = ZUTAT.ZUTATENNR 
JOIN REZEPTE 
	ON REZEPTEZUTAT.REZEPTNR = REZEPTE.REZEPTNR;

---------------------------------------------------------------------
<------- join tables REZEPTEZUTAT, ZUTAT and REZEPTE (with Filter) [WORKS WITH REAL DATA]----->
SELECT
	ZUTAT.ZUTATENNR,
    ZUTAT.BEZEICHNUNG,
    ZUTAT.KALORIEN,
    ZUTAT.KOHLENHYDRATE,
    ZUTAT.PROTEIN,
    REZEPTEZUTAT.MENGE
FROM ZUTAT
JOIN REZEPTEZUTAT 
	ON ZUTAT.ZUTATENNR = REZEPTEZUTAT.ZUTATENNR AND REZEPTEZUTAT.REZEPTNR = 1

---------------------------------------------------------------------
//* = Zusätzliche vorraussetzungen können geschaffen werden indem hier 'OR {additional argument}' angehängt wird
---------------------------------------------------------------------